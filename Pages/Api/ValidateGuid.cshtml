@page "/api/ValidateGuid"
@model App1Simulation.Pages.Api.ValidateGuidModel
@{
    // This file is intentionally empty.
    // This Razor Page acts as an API endpoint and returns JSON directly from its OnPostAsync method.
    // No HTML rendering is needed.
    Layout = null; // Ensure no layout is applied for API endpoints
}
